name: tests
on:
  push:

jobs:
  promote_model:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
          
      - name: Check schema
        run: |
          ./schema_check.sh
