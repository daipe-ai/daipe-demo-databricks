imports:
  - config.yaml

parameters:
  databricks:
    address: https://westeurope.azuredatabricks.net
    workspace:
      base_dir: '/daipedemo'

  datalakebundle:
    notebook:
      display:
        enabled: False
